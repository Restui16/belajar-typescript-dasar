describe('Data Type', function (){
    it('should must declare', function(){

        const name: string = 'Restu Wibowo'
        const num: number = 100000
        const isVip: boolean = true

        console.info(name);
        console.info(num);
        console.info(isVip);
        
    })
})